\documentclass[border={20pt 5pt 105pt 10pt}]{standalone} % Left Bottom Right Top
%\documentclass{standalone} 

%\usepackage{verbatim}
%\usepackage{parcolumns}
%\usepackage{fancyvrb}

\usepackage{xparse}
\usepackage{listings}
\usepackage[dvipsnames]{xcolor}
\usepackage{amsmath} %for \text{} macro
\usepackage{wasysym} %for \lightning symbol
\usepackage{amssymb} %for \circlearrowright symbol

\lstdefinestyle{mystyle}{
	tabsize          = 3,     						% tab space width (for "for" loop for instance)
	numbers          = left,  						% display line numbers on the left
	numberblanklines = false,
	%framesep         = 0pt,   						% expand outward
	%framerule        = 0pt, 						% expand outward
	frameround		 = ffff,
	frame 			 = single,
	commentstyle     = \color{ForestGreen},      	% comment color
	keywordstyle     = \bfseries\color{blue},       % keyword color
	stringstyle      = \color{DarkRed},  	  		% string color
	backgroundcolor  = \color{white}, 				% backgroundcolor color
	showstringspaces = true,             	 		% mark spaces in strings
	otherkeywords	 = {NV, signal,inos_task,ios_vars, uint8_t, type},
	%numbers = none,
	firstnumber=1,
	language = C,
	escapechar = !,
	basicstyle = \ttfamily,
	columns = flexible,
	mathescape = true, %activates $ sign
	morecomment = [l]{//}
}

\begin{document}	

\begin{lstlisting}[style = mystyle]
_p type pvar1, $\ldots$, _p type pvar$i$;

void task$i$(void){type nvar1, $\ldots$, type nvar$i$
task_body }
$\ldots$
int main(void){
	init(); //system initialization
	taskID taskName[] = {{task1}, {task2}, 
		$\ldots$,{task$i$}}; /list of tasks for state machine
	addTask(taskName); //create state machine
	scheduler(); //schedule tasks
	return 0;}
\end{lstlisting} 

\end{document}